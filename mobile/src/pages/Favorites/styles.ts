import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1, // faz com que o objeto ocupe a tela toda
        backgroundColor: '#f0f0f7',
    },
    teacherlist:{
        marginTop: -40,
    }
});

export default styles;